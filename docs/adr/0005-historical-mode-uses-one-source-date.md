# Historical mode uses one source date

Historical Mode selects one historical source date for a run and draws its rounds from NBA games on that date. This keeps the experience coherent around a single NBA slate instead of mixing unrelated games across seasons, while still allowing the system to sample which eligible date is used.
